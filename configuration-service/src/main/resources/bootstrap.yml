server.port: 8888

spring:
  application:
    name: configserver
    index: ${random.int}
  cloud.config:
    server:
      git.uri: git@github.com:marcosbarbero/netshoes-bootiful-config-repo.git
      bootstrap: true
